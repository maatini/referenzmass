# ReferenzMaß — Projektstatus 2026-05-23 (Update 16:50)

> **Historischer Snapshot.** Nicht fortschreiben. Aktueller Stand: [`HANDOFF.md`](../HANDOFF.md). Agent-Regeln: [`AGENTS.md`](../AGENTS.md).
>
> 2026-09-04: Sprint 1–2 sind im Code bestätigt. Sprint 3–6 weiterhin offen. Zusätzliche P0-Bugs (Serde-Mismatch, Load-`$effect`, Display-Pixel-Koordinaten) stehen nicht in diesem Dokument — siehe `AGENTS.md`.

## Sprint 1: Bug-Fix + Cleanup ✅ (abgeschlossen)
- `greet`-Boilerplate aus `lib.rs` entfernt
- `jsconfig Kopie.json` gelöscht
- Rust `CalibrationState`: `calib_type` + `homography` + Plane-Roundtrip-Test
- Frontend: `CalibrationState` erweitert, `stateToCalibration()` neu, `loadProjectState()` repariert
- Tests: 40 Vitest + 3 Rust — alle grün

## Sprint 2: E2E-Test-Lücken ✅ (abgeschlossen)

### Neue Specs (3 Dateien)

#### `e2e/interactions.spec.ts` — 7 Tests
- Scroll-wheel zoom (kein Crash)
- Space-drag pan (cursor wechselt `grab` ↔ `default`)
- „Reset Ansicht" Button sichtbar und klickbar
- „Reset Ansicht" nach Kalibrierung (kein Crash)
- Kalibrierungs-Reaktivität (Referenzlänge ändern → Scale)
- Messlinie zeichnen → korrekte Länge (5.00 cm)
- Kalibrierungsmodus Plane → Line zurückschalten

#### `e2e/loupe.spec.ts` — 4 Tests
- Loupe erscheint NICHT ohne Bild
- Loupe erscheint beim Zeichnen auf geladenem Bild
- Loupe hat korrekte Struktur (`.crosshair`, `border-radius: 50%`)
- Loupe verschwindet bei `mouseleave`

#### `e2e/edge-cases.spec.ts` — 11 Tests
- Sehr kurzer Drag (< 5px) → keine Referenzlinie
- `realWorldLength = 0` → Kalibrierung entfernt
- „Clear line" Button → State reset
- „Clear plane" Button → „Keine Referenzfläche (0/4)"
- „Add Measurement Line" unsichtbar ohne Kalibrierung
- „Export CSV" nur sichtbar mit Messungen
- „Export CSV" erscheint nach Messung
- Delete ohne Selektion → kein Effekt
- Hint: „Zeichne zuerst die Referenzlinie"
- Hint: „Klicke oben auf Messung hinzufügen"
- Status-Toast: „Testbild geladen"

### Erweiterungen bestehender Specs

#### `e2e/app.spec.ts` — +1 Test
- „Klicke oben auf Messung hinzufügen"-Hint nach Kalibrierung

### Testergebnisse (Stand 16:50)

| Suite | Dateien | Tests | Ergebnis |
|---|---|---|---|
| Vitest | 12 | 40 | ✅ |
| Rust (`cargo test`) | 1 | 3 | ✅ |
| Playwright E2E | 6 | 33 | ✅ |

---

## Ausstehend

| Sprint | Bereich | Geschätzt |
|---|---|---|
| 3 | CSV-Export-Test + Homographie-Tests | ~2h |
| 4 | CalibrationCanvas Refactoring | ~4h |
| 5 | Unit-Tests für UI-Komponenten | ~3h |
| 6 | Undo/Redo (optional) | ~4h |
